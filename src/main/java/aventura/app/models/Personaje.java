package aventura.app.models;

/**
 * Clase abstracta Personaje.
 * Sirve como base para todos los personajes del juego (jugador, enemigos, NPCs, etc.).
 * Al ser abstracta, no se puede instanciar directamente, pero permite que las clases hijas
 * hereden su comportamiento y atributos comunes en un futuro.
 */
public abstract class Personaje {
    // Actualmente no tiene atributos ni métodos,
    // pero puede ser extendida para agregar propiedades como vida, fuerza, inventario, etc.
}
